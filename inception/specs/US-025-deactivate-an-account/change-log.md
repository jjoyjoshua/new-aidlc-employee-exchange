# US-025 — change log

> The curated history of this spec: what changed and why, in the words of whoever changed it. Git holds every edit; this holds the ones that mattered. A Medium-tier change to an existing package appends a row here.

| Date       | Change                                                                                                                                                                                                                                                       | Why                                                                                                                                                          | Requirements affected |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- |
| 2026-09-20 | Spec package created (spec, impact analysis, decisions, traceability, implementation plan). Scoped down before drafting: the story's original AC-08 (cancellation email) and AC-09 (push alert) **moved out of `US-025-deactivate-an-account.md`** into US-029/US-032, which already own the identical behaviour and already cite this story by id (D-01, [`jjoyjoshua/new-aidlc-employee-exchange#59`](https://github.com/jjoyjoshua/new-aidlc-employee-exchange/issues/59)) | Discovered mid-planning that `aidlc-check` requires the manifest's `acs[]` to match the story file's ACs exactly, with every listed AC needing a passing test once any test exists for the story — there is no "listed but deferred" state a PR can merge with, so leaving AC-08/AC-09 in US-025 untested was never viable. Moving them to where they were already specified, rather than building a notifications stub mid-story, was confirmed with the human | FR-01–FR-11 added; FR-12 added (the RPC's forward-compatible return shape); story file's AC-08/AC-09 removed |
| 2026-09-20 | **Gate D1 approved** (see `implementation-plan.md`'s Approval block — Joy Joshua, uncommitted at approval, base `44fa117`). Next: the Architect design note settles Step 2's exact cascade-function SQL, per `ai/gates/delivery.md`'s ordering, before Step 1 is coded | Human reviewed the plan and impact analysis in chat and replied `go` | none yet — implementation not started |
| 2026-09-20 | **Architect design note received** (`design-note.md`) and applied, before any implementation. Two blockers: the migration must `revoke`/`grant` `execute` on the new function (it is otherwise reachable at a public `/rpc/` URL with the browser's anon key) and must have no `EXCEPTION` block (or the last-admin refusal's abort would be swallowed). A fourth repository outcome, `already_inactive`, was added — the sketched two-outcome shape had nowhere to put a target that was already inactive when the cascade ran, and `not_found`'s approved 404 copy would have been false about it (D-05 collapses it to `ok` in the service). `spec.md`'s Out-of-scope bullet and `decisions.md`'s open item were corrected: `require-session.ts` already refuses a deactivated account's live session at its next request, which is narrower than "does a live session survive deactivation" implied. `apps/api/src/composition.ts` and `apps/api/src/modules/bookings/README.md` added to `impact-analysis.md`'s Files table (both were missing). D-02's rationale corrected to cite `app-architecture.md` §2's written exception rather than "the write happens inside a function." Step 2a gained a fourth gated case (two concurrent deactivations of the last two active admins) — the one that actually proves the trigger fires from inside a PL/pgSQL function and that the abort reaches the `bookings` table. No new ADR — the concurrency mechanism is already ADR-013's, and the single-transaction-vs-compensation choice is already ruled by ADR-011/ADR-012's own scope. Three new open items for the human (self-deactivation copy, the `already_inactive`-vs-409 alternative, a residual where a booking made in the milliseconds around a cascade can survive it) and confirmation that Figma frames were not read for SCR-008 ST-05–07 | Complex tier's own ordering: a design note is written after D1, before code | FR-02 reworded (four outcomes); FR-12 reworded (exact return shape); Technical constraints and Out-of-scope reworded in `spec.md`; D-02 corrected, D-05 added in `decisions.md`; Files table extended in `impact-analysis.md`; Steps 1–5, 8 and Open questions reworded in `implementation-plan.md` |
| 2026-09-20 | **Figma frames read for SCR-008 ST-05/ST-06/ST-07** (node-ids 252-1291/1354/1394 [ST-05], 252-1466/1508/1548 [ST-06], 252-1605/1689/1750 [ST-07] — desktop/tablet/mobile each) — the frames match the written spec exactly: ST-05's "They won't be able to sign in. Their past bookings are kept." with plain "Deactivate"; ST-06 lists each booking individually (desk + date) and carries the count in the confirming action's own label ("Deactivate and cancel 3 bookings"), states the desks return to the pool and the person is emailed; ST-07 names the consequence in full and routes to "Make someone an admin". No discrepancy to escalate — design note open item 6 resolved | Human provided the real design file after the Architect's own note flagged it had none; per this session's standing rule to confirm real designs before UI work | none — confirmation only |
| 2026-09-20 | **Steps 1–5 implemented.** Contract (`deactivationPreviewSchema`, no `count` field), migration `0005_deactivate_account_cascade.sql` (the design note's SQL verbatim — no `EXCEPTION` block, `revoke`/`grant` on `execute`, `p_now`/`p_today` as parameters, `already_inactive` as a fourth outcome), `usersRepository.previewDeactivation`/`deactivateAccount`, `usersService.previewDeactivation`/`deactivateAccount` (collapses `already_inactive` to `ok`), `officeTimezone` added to `UsersServiceDeps`/`composition.ts`, two new admin routes (the deactivate route uses `requireActingAdmin`, deliberately unlike the role route). **Migration applied to the `yxpfzlzpmmtydejjhsls` Supabase project** (human's explicit go-ahead) and the gated `admin.concurrency.spec.ts` suite (Step 2a, 4 new cases) run against it — all 14 cases green (10 US-024 + 4 US-025), including case 4 (two simultaneous deactivations of the last two active admins leave exactly one active admin, the loser's account AND its bookings both untouched) — proving the trigger fires correctly from inside the new function and that its abort rolls back both tables, not just `user_profiles`. Two fixture bugs found and fixed during the first live run: case 2's four bookings needed four distinct dates (`bookings_one_confirmed_per_user_per_day`), and case 3 needed a second active admin to remain after the target's deactivation; a batched-cleanup ordering bug (booking ids pushed to cleanup only after all inserts succeeded, leaking rows past `finally` on a partial failure) was fixed with a `bookOrThrow` helper that registers cleanup immediately. Orphaned fixture rows from the first failed run were manually deleted before re-running; a post-run check confirmed no fixture data remains. `npm run test -w apps/api` — 613 passing, 16 skipped (workspace-wide, without the gate flag); `npx tsc --noEmit` clean | Steps land test-first per plan; the live-database step needed explicit confirmation before touching a shared resource | FR-01–FR-11 implemented |
| 2026-09-20 | **Steps 6–9 implemented.** Frontend fetchers (`deactivate-account.ts`), the dialog hook (`use-deactivate-account-dialog.ts`, extending `use-role-change-dialog.ts`'s state machine with a preview-loading phase and a `preview_failed` outcome for when AC-05's preview cannot be read), `DeactivateAccountDialog.tsx` (ST-05/ST-06/ST-07/ST-12/ST-13), `AccountRowMenu`'s deactivate branch made live (the activate branch stays disabled), `useUsers.markDeactivated` (moves `summary.deactivated` only), and `People.tsx` wiring. `traceability.md` filled with real file locations; `knowledge/traceability/manifest.json` `tests[]` populated; `apps/api/src/modules/users/README.md` and `modules/bookings/README.md` updated per the design note's documentation obligations (C17, C18); `inception/specs/index.md` and `spec.md`'s Status moved to `implemented`. Fixed three AC citations the traceability checker caught as unproven (a comma-separated AC list only credits the FIRST id repeating the `US-025/` prefix, not every id after it — `tools/aidlc-check.mjs:104-108`'s exact-substring match) and removed two incidental bare `US-026` mentions from `AccountRowMenu.spec.tsx` that tripped the stale-manifest rule. `node tools/aidlc-check.mjs` clean; `npm run lint` clean; `npm run typecheck` clean workspace-wide; `npm test` — contracts 268, api 613 (+20 gated, skipped without the env flag; one unrelated pre-existing timing test, `auth.routes.spec.ts`'s sign-in-floor check, flaked once under full-suite load and passed clean on every isolated re-run), ui 963 — all green | Steps land test-first per plan; the traceability/lint/typecheck/test gates are Gate D2's own requirements | FR-01–FR-12 fully implemented; story complete, ready for Gate D2 (PR review) |
