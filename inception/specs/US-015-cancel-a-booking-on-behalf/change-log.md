# US-015 — change log

> The curated history of this spec: what changed and why, in the words of whoever changed it.

| Date | Change | Why | Requirements affected |
| --- | --- | --- | --- |
| 2026-09-19 | Package created. Classified Complex — a new write route, `POST /api/admin/bookings/:id/cancel` (`ai/standards/task-surfaces.md:39`), and a cross-employee write path; no migration needed, since the schema already carries `cancellation_source`/`cancelled_by` | AC-05/AC-06's attribution and the write's own concurrency guard both depend on columns placed by an earlier migration specifically for this story | all |
| 2026-09-19 | Confirmed the Figma designs for SCR-005 ST-07–ST-11 (5 states × 3 breakpoints) before any UI work, per standing feedback to always verify the real design file rather than the written screen spec alone. Confirmed every visual element is composable from existing components (`ConfirmDialog`, `Button`, `StatusChip`) with no new component or prop needed | Avoids the earlier incident where UI was built from the prose spec alone and had to be redone | FR-09, FR-11, FR-12 |
| 2026-09-19 | Architect design note requested and written (`design-note.md`) — settled the endpoint shape, the write's placement on `AdminBookingsRepository`, the no-row-version concurrency proof, the AC-05/AC-06 data-only handling, and the no-refetch requirement for AC-04 | Complex tier blocks on a design note before code (`ai/gates/delivery.md`) | all |
| 2026-09-19 | Resolved three of the design note's open items directly with the human: the past-dated cancel folds into the existing `404` with no new error code (D-07); the "already cancelled" dialog keeps its approved copy naming the employee even though the actor can't always be verified (D-08); the disclosure-rule standards edit lands in this PR rather than being carried forward again (D-09). Adopted the Architect's recommendation as a DEV default for the remaining three non-blocking items: skeleton height 188px (D-04), full employee name throughout copy (D-05), and the focus-return-to-row fix (D-06) | Auto-mode guidance: resolve genuine trade-offs with the human directly; proceed on a reasonable, cheaply-reversible default for cosmetic/copy items rather than stall Gate D1 | D-04 through D-09 |
| 2026-09-19 | Implemented Steps 1–13. `npm run lint`, `npm run typecheck`, and `npm test` (976 tests across `apps/api`, `apps/ui`, `libs/contracts`) all green; `node tools/aidlc-check.mjs` green. Two AC citations needed moving from a `describe` title into the actual `it`/`test` title for `aidlc-check`'s parser to see them (AC-03, AC-10) — caught immediately by the check itself, not a defect in the shipped behaviour | Test-first development and the framework's own tooling caught both issues before they could reach review | all |
| 2026-09-19 | Two verification gaps, disclosed rather than hidden: (1) `bookings.repository.concurrency.spec.ts`'s two new gated tests (NFR-01) are written and typecheck cleanly, but this environment has no disposable Supabase/Postgres project to run `RUN_BOOKINGS_CONCURRENCY_TEST=1` against — they remain **skipped**, not run, and the design note's own real-Postgres proof of AC-07/AC-09's arbitration is therefore unverified here; (2) no live manual check of ST-07–ST-11 against the running app in a browser (Step 13's last item) — this environment has no backing database to sign in against. Both are flagged to the human as follow-ups before merge, not silently skipped | Honesty about verification gaps matters more than a clean-looking checklist; `ai/gates/delivery.md`'s test-first rhythm assumes gated/manual steps are surfaced, not assumed away | NFR-01 |
