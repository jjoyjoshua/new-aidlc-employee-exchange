# US-012 — change log

> The curated history of this spec: what changed and why, in the words of whoever changed it.

| Date       | Change                                                                 | Why                                                                                   | Requirements affected |
| ---------- | ------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------- | ----------------------- |
| 2026-09-19 | Package created; plan scoped AC-06 to SCR-002 only, SCR-005 tracked as a follow-up | `AllBookings.tsx` is a stub pending US-013 — nothing there to refresh yet; confirmed with the human before writing the plan | AC-06 |
| 2026-09-19 | **Implementation halted after Step 1.** `implementation-plan.md`'s Medium-tier, per-screen `useFocusRefresh` design is superseded — re-tiered to Complex, blocked on an Architect design note. The per-screen hook and its test (`use-focus-refresh.ts`/`.spec.ts`) were removed from the working tree | `apps/ui/src/lib/README.md:6-8` states the data-fetching layer (not yet built) owns REQ-036's refresh-on-focus "configured once for the whole app — never a per-screen `useEffect`", and "changing its configuration is Complex" — found only after Step 1 was already coded and green, too late to have caught at classification | all |
| 2026-09-19 | **Resumed at Complex tier.** Added `spec.md` (FR-01–FR-09) and `impact-analysis.md`; rewrote `implementation-plan.md`'s steps around ADR-008's shared `apps/ui/src/lib/data-refresh.ts` module in place of the removed per-screen hook; `decisions.md` D-02 annotated as superseded in placement (not reasoning) by ADR-008; `traceability.md` rebuilt against `FR-##` rows as the template requires at Complex tier. Gate D1 approval reset to `awaiting review` — the technical approach changed, so the earlier chat `go` does not carry over | Architect accepted the shared-module design in [ADR-008](../../../knowledge/decisions/ADR-008-focus-refresh-shared-module.md); the human then invoked `/dev` to resume | all |
| 2026-09-19 | **Implemented.** Gate D1 stamped approved; all 6 steps built test-first (`data-refresh.ts`, `use-my-bookings.ts`'s `refreshQuietly`, `copy.ts`'s `QUIET_REFRESH_FAILED`, `MyBookings.tsx` wiring); `traceability.md` flipped to `implemented` with real `file:line`; manifest and spec index updated. `npm run lint`, `npm run typecheck`, `npm test` all green (371/371 in `apps/ui`, no regressions) | Human said `go` on the re-tiered Complex plan | FR-01–FR-09 |
