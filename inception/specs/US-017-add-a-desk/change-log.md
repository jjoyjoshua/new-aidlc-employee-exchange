# US-017 — change log

> The curated history of this spec: what changed and why, in the words of whoever changed it. Git holds every edit; this holds the ones that mattered. A Medium-tier change to an existing package appends a row here.

| Date       | Change                                 | Why                    | Requirements affected        |
| ---------- | ----------------------------------------- | -------------------------- | ----------------------------- |
| 2026-09-19 | Package created: `spec.md`, `implementation-plan.md`, `impact-analysis.md`, `decisions.md`, `traceability.md`, this file, on top of the Architect's `design-note.md`. Two decisions surfaced by the design note (the SCR-007 status-radio conflict, and the `Dialog` extraction scope) were put to the human and resolved before the plan was finalised — see `decisions.md` D-01, and Step 5 of `implementation-plan.md` | DEV picks up US-017 (add a desk), the story queued behind US-016's now-merged desk inventory | FR-01 through FR-11, NFR-01, NFR-02 added |
| 2026-09-19 | Filed [issue #49](https://github.com/jjoyjoshua/new-aidlc-employee-exchange/issues/49) (`change-request`) to reconcile SCR-007's approved status-radio frames against US-017's story, which has no AC for one | The two Gate 1 artifacts disagree and neither this story nor DEV may resolve it unilaterally; tracked separately so it does not block this PR (`decisions.md` D-01) | none — process only |
| 2026-09-19 | All 12 implementation-plan steps completed, test-first: the shared desk-number rule and write contract, the repository insert and its `23505`/`23514` mapping, the service, `POST /api/admin/desks`, the extracted `Dialog` shell (with `ConfirmDialog.spec.tsx` staying green unedited), `TextField`'s `helper` prop, the `add-desk` fetcher and `markAdded`, `use-add-desk-dialog`, `DeskFormDialog`, and `Desks.tsx`'s wiring. `traceability.md`, `spec.md` and this package's status updated to reflect it. Full workspace `npm test` and `npm run typecheck` green | Gate D1 `go` received; ready for the human to commit, push, and open the story PR | FR-01 through FR-11, NFR-01, NFR-02 — all implemented |
| 2026-09-19 | `traceability.md`'s FR-06 row and Key symbols table repathed: `use-add-desk-dialog.ts`/`useAddDeskDialog` → `use-desk-form-dialog.ts`/`useDeskFormDialog` | US-018 generalised this hook to cover its own edit mode and renamed the file in the same PR (design note §6.2); the old path no longer exists, and a table pointing at a deleted file is not a record | none — path correction only |

A plan edited after its Gate D1 approval **must** have a row here **dated on or after the approval date**. Check 16 compares the plan against the approved SHA and fails a silent change; an older row does not cover a newer edit.
