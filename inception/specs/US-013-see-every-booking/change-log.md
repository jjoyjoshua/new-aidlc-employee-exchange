# US-013 — change log

> The curated history of this spec: what changed and why, in the words of whoever changed it.

| Date       | Change                                                                 | Why                                                                                   | Requirements affected |
| ---------- | ------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------- | ----------------------- |
| 2026-09-19 | Package created. Classified Complex (a new route, and a new `libs/contracts` slice — `ai/standards/task-surfaces.md:39`, `:25-27`), corrected up from an initial Medium read | Every prior story in this repo (US-001–US-012) was Complex for the same class of reason; the project's own task-surfaces extension makes "a new route" Complex outright, with no read-only carve-out surviving once the response shape is genuinely new | all |
| 2026-09-19 | Pulled the real hi-fi Figma frames for ST-01/02/03/05 at 1280/768/360 before writing the plan, per the user's standing preference to confirm designs before UI work | The written SCR-005 spec alone left row heights, exact column widths and the empty/error copy unverified; the frames gave exact pixel/token measurements (64px table row, 80px/160px card rows, exact result-summary and alert copy) | FR-07, FR-08, FR-09, FR-10 |
| 2026-09-19 | Architect design note requested and written (`design-note.md`) — settled the endpoint contract, pagination shape, FK-disambiguation approach, and confirmed two DEV scope calls (no action column; simplified ST-03) | Complex tier blocks on a design note before code (`ai/gates/delivery.md`) | all |
| 2026-09-19 | Three design-note open items resolved with the human in chat: `total` is the full matching count (D-01); pagination precedent recorded as a standards edit, not an ADR (D-02); `MAX_PAGE` kept (D-03). A fourth (real-Postgres verification) deferred to Step 6, credentials to follow out of chat | Gate D1 needs no unanswered load-bearing fact before the plan is presented | FR-02, FR-03, FR-04 |
| 2026-09-19 | Two more open items resolved directly against the already-pulled Figma frames rather than escalated: the 360px skeleton height is 160px, not SCR-005's stated 188px, because this story's rows never carry the cancel-button variant that 188px belongs to (D-04); the AC-11 table/card switch renders both trees and lets CSS show one (D-05, matching the Architect's own recommendation) | The frames were already in hand; escalating a question the data already answers would cost a round trip for nothing | FR-08, FR-09 |
| 2026-09-19 | Implemented Steps 1–5 (contracts, repository, service, router, composition) and Steps 7–10 (UI). Ran Step 6's gated real-Postgres verification against the project's disposable Supabase project (confirmed in chat first) — both cases passed, but the second one surfaced a real gap the fake-client tests could not: PostgREST answers a page past the last matching row with `PGRST103`, not an empty result. Added the mapping (`decisions.md` D-10) and a fake-client regression test for it | The design note's open item 2 named this exact risk and routed it through the existing gated harness before merge; the harness did its job | FR-01–FR-14, D-10 |
| 2026-09-19 | `npm run lint`, `npm run typecheck`, `npm test` all green across `libs/contracts`, `apps/api`, `apps/ui` (one transient timing-test failure under load, reproduced as a pass in isolation — pre-existing, unrelated to this story) | Step 12 verification | all |
