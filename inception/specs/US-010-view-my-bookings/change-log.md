# US-010 — change log

> The curated history of this spec: what changed and why, in the words of whoever changed it. Git holds every edit; this holds the ones that mattered. A Medium-tier change to an existing package appends a row here.

| Date       | Change                                                                                                                                          | Why                                                                                                                                                                             | Requirements affected |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------ |
| 2026-09-18 | Package created: `spec.md`, `impact-analysis.md`, `decisions.md`, `traceability.md`, `implementation-plan.md`, `change-log.md`; `design-note.md` written by the Architect persona first; `ADR-007` drafted alongside it | Gate D1 for US-010. The design note settled the API shape and the Completed-derivation mechanism, both of which the story text explicitly defers to `/architect`. Attempting to open the linked Figma file (`xjFVgBbMrJUl7Ys3EX3Cbn`) for SCR-002's hi-fi frames found only a `Cover` page — no mockup frames under it — logged as a blocking open question in `implementation-plan.md` rather than assumed away | FR-01–FR-15 |
| 2026-09-18 | Gate D1 approved by Joy Joshua after working Figma links were supplied and the frames matched the written spec exactly; three remaining open items resolved in chat (`implementation-plan.md`); `decisions.md` D-03 revised (real `icon-close.svg` downloaded, not hand-drawn) | Unblocks Steps 7–8 (the UI build) | — |
| 2026-09-19 | Implementation landed: Steps 1–9 of `implementation-plan.md` — contracts, `domain/booking-history.ts`, the two new repository reads, `bookings.service.listMyBookings`, `GET /api/bookings`, `StatusChip`'s booking variants, `BookingRow`, `use-my-bookings`, and `MyBookings.tsx`'s full build-out. `traceability.md` moved every `FR-##`/`NFR-##` to `implemented`. All tests, lint, typecheck and `npm run build` green; `aidlc-check` clean. Two implementation-time decisions added: D-06 (a failed `loadOlder()` leaves the list untouched) and D-07 (Past bookings is a native `<details>`/`<summary>` disclosure, not a stateful Accordion) | FR-01–FR-15, NFR-01–NFR-03 |

A plan edited after its Gate D1 approval **must** have a row here **dated on or after the
approval date**. Check 16 compares the plan against the approved SHA and fails a silent change;
an older row does not cover a newer edit.
