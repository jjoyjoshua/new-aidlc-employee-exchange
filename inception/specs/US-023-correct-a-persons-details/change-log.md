# US-023 — change log

> The curated history of this spec: what changed and why, in the words of whoever changed it. Git holds every edit; this holds the ones that mattered. A Medium-tier change to an existing package appends a row here.

| Date       | Change                                                                                                    | Why                                                                                                                                                                              | Requirements affected |
| ---------- | ---------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------- |
| 2026-09-20 | Spec package created (spec, impact analysis, decisions, traceability, implementation plan). Confirmed the Figma hi-fi frames for SCR-009's edit states (ST-02, ST-03, ST-04, ST-05, ST-08) against the written screen spec before drafting | US-023 is next in the approved backlog after US-020/US-021; both are merged | FR-01–FR-09 added |
| 2026-09-20 | Architect design note (`design-note.md`) and a new ADR (`ADR-012-update-compensation-across-systems.md`) received and applied **before** Gate D1 — unlike US-021, where the design note followed approval. Constraints C1–C19 folded into `spec.md` (Technical constraints, Out of scope), `impact-analysis.md` and `implementation-plan.md`'s steps | US-023 was classified Complex on a genuine architectural question (cross-system update ordering and compensation, which ADR-011 forecast but did not answer) that DEV is not authorised to decide solo; the design note resolves it before any code is written, not after | Technical constraints added; Out of scope extended (session termination, re-notification, operator repair path) |
| 2026-09-20 | Implementation landed (Steps 1–10) and manually verified against the live Supabase project: an edit closes the dialog with the "{fullName} updated." toast and the row updates in place; a duplicate email is refused naming the holder; saving with the email unchanged succeeds; an implausible email is caught client-side with no request sent; and — the previously-unverifiable question — the OLD email stops signing in and the NEW email signs in immediately with the unchanged password, confirming `email_confirm: true` is sufficient (open item 3 resolved). `spec.md` FRs, `traceability.md` and the manifest updated to `implemented`; `users/README.md` gained the `updateAccount` section (constraint C16) | `aidlc-check` clean; 1652 automated tests green (557 API, 838 UI, 257 contracts) | traceability.md and spec.md FR statuses moved to implemented; manifest `tests[]`/`decisions[]` filled in |
