# US-001 — change log

> The curated history of this spec: what changed and why, in the words of whoever changed it. Git holds every edit; this holds the ones that mattered. A Medium-tier change to an existing package appends a row here.

| Date       | Change                                                                                                                                          | Why                                                                                                                                                                                                | Requirements affected                              |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------- |
| 2026-09-17 | Package created — `spec.md`, `implementation-plan.md`, `impact-analysis.md`, `decisions.md`, `traceability.md`, this file, and the Architect's `design-note.md` | First story of delivery, classified Complex. Written before any code, per `ai/gates/delivery.md` Gate D1                                                                                            | FR-01 – FR-32, NFR-01 – NFR-05 created             |
| 2026-09-17 | Sign-in moved from the browser to Express (ADR-003)                                                                                              | AC-04 requires an unknown email, a wrong password and a deactivated account to share one message **and** one response-time band. Supabase Auth has no concept of `is_active`, so a browser signing in directly receives a real token for a deactivated account and discovers the refusal one round-trip later, at a measurably different time. Approved by Joy Joshua | FR-06 – FR-15 shaped by it; FR-13 exists because of it |
| 2026-09-17 | `react-router-dom` adopted                                                                                                                       | AC-01, AC-02 and AC-03 assert on addresses and `apps/ui` had no router. Dependency approved by Joy Joshua (US-001/D-01)                                                                             | FR-28, FR-30 unblocked                             |
| 2026-09-18 | `apps/ui/src/styles/index.css` gained a base layer: `box-sizing: border-box`, `body { margin: 0 }`, `color-scheme: light dark`, `font: inherit` on controls | Found by Joy Joshua looking at the running screen. Without `border-box`, every control with padding computed `width: 100%` **plus** its padding and border — the fields hung 34px over the card edge. The browser's default 8px body margin drew a white frame around the dark page. **No test could catch either: jsdom has no layout engine and no browser chrome.** That gap is what US-033 exists for | FR-32, NFR-05 |
| 2026-09-17 | `isTransportFailure` now treats any status below 100 — including supabase-js's `status: 0` — as an outage | A refused connection returns `AuthRetryableFetchError` with `status: 0`. The guard only checked for `undefined`, so `0 >= 500` was false and a **total outage was classified as a credential rejection**: every user told their password was wrong during an incident, and ST-05's Try again never offered. Found by pointing the server at a Supabase that was not running | FR-14 corrected |
| 2026-09-17 | `ws` declared in `apps/api` and supplied as the Supabase realtime transport (D-14) | `createClient` threw at construction on Node 20 — no global WebSocket — so the server could not build a Supabase client at all and every sign-in returned 500. Verified fixed against the running server: the three AC-04 causes now return one byte-identical 401 at ~0.52s each | none — unblocks FR-08 at runtime |
| 2026-09-17 | ADR-003's consequential edits applied (step 13): ADR-001 §Decision, `app-architecture.md` §1, `coding-standards.md` §Browser and `apps/ui/src/lib/supabase-client.ts` all narrowed to "refreshing the token"; `security-standards.md` gained a §Sign-in section; `api-standards.md` gained the `503` row | All four said the browser signs in — the opposite of how the system now works. The client's own docblock was the worst of them: it told a developer to sign in with a client that must not | none — documentation correctness |
| 2026-09-17 | `traceability.md` brought up to date and `manifest.json` `tests[]` filled with 16 files | The table had gone stale while the UI was built — FR-23 – FR-32 still said `not started` with the code and tests already written. A table reconstructed after the fact is fiction, which is why the charter requires it in the same commit as the code | FR-19, FR-23 – FR-30, NFR-03 – NFR-05 status corrected |
| 2026-09-17 | `auth.adapter.ts` guards against a Supabase client that **throws** rather than returns an error | Found by running the server: a throw escaped the adapter and surfaced as `500 internal_error` — "our bug" — when the truth was "we cannot reach the service". A whole class of downstream failure was reading as AC-04 rather than AC-07 | FR-14 widened |
| 2026-09-17 | Design reference added to the plan — Figma file `xjFVgBbMrJUl7Ys3EX3Cbn`, node `139:3` (SCR-001 ST-01 at 1280) and the five component nodes, with D-11 (no Tailwind) and D-12 (backdrop ships from the repo SVG) | Joy Joshua supplied the design link. An earlier check of the same file listed only a Cover page and I reported the frames as missing; that was wrong — the frames exist and resolve by node id, and their variables are bound to the same names as `tokens.css` | FR-32 gains a visual target; FR-23 – FR-27 gain their frames |
| 2026-09-17 | Scope stated explicitly: route stubs at `/bookings` and `/admin/bookings` plus the shell's role-dependent navigation                              | AC-01 and AC-02 land on two screens and AC-02 requires the admin navigation present; AC-03 requires an Employee to be bounced off an admin address. Named here so it is not discovered at review as scope creep | FR-28, FR-29, FR-30 added                          |
| 2026-09-18 | Bugfix (Medium tier): `onSession` gained a `defaultOnSession`, wired at both call sites in `auth-context.tsx` | FR-28 was marked `implemented` but never actually persisted the session — `App.tsx` mounts `AuthProvider` with no props, so `onSession?.(...)` silently no-opped and a page refresh always signed the user out. Found by Joy Joshua reporting the refresh-logs-out symptom. Full addendum in `implementation-plan.md` | FR-28 corrected; restores US-003/AC-01 in production, not only under test |

A plan edited after its Gate D1 approval **must** have a row here **dated on or after the
approval date**. Check 16 compares the plan against the approved SHA and fails a silent change;
an older row does not cover a newer edit.
