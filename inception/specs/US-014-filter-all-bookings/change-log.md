# US-014 — change log

> The curated history of this spec: what changed and why, in the words of whoever changed it.

| Date       | Change                                                                 | Why                                                                                   | Requirements affected |
| ---------- | ------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------- | ----------------------- |
| 2026-09-19 | Package created. Classified Complex — `libs/contracts` is a protected path (`ai/standards/task-surfaces.md:25-27`) and the desk filter needs a new route, `GET /api/admin/desks` (`task-surfaces.md:39`); on their own the repository/service/UI changes here would each be Medium | The contract change alone (adding four optional query fields to `allBookingsQuerySchema`) forces Complex regardless of diff size, independent of the new-route surface | all |
| 2026-09-19 | Architect design note requested and written (`design-note.md`) — settled AC-08's build-the-receiving-half-only shape, the desk list's placement as a new `GET /api/admin/desks` resource in a new `modules/desks`, the compound status-predicate rule, and confirmed the response envelope needs no change | Complex tier blocks on a design note before code (`ai/gates/delivery.md`) | all |
| 2026-09-19 | Adopted the design note's recommendations as working defaults for six open items (D-01 through D-06 in `decisions.md`); four of those (D-04 through D-06, plus the collapse-breakpoint pixel value and the desk-select first-load treatment) are explicitly not yet confirmed with UX/PO and are carried as non-blocking open questions in `implementation-plan.md` | Auto-mode guidance: proceed on a reasonable default rather than stall Gate D1 on copy/pixel decisions that don't change the plan's shape and are cheap to correct after merge | D-01–D-06 |
| 2026-09-19 | Implemented Steps 1–13, all green (lint/typecheck/331+453+140 tests). A round-trip property test caught a real bug during Step 4 (TDD): the service's "absent `from` defaults to today" rule, applied under `status=completed`, would have made every Completed-only query structurally empty — fixed by making `from` optional end to end and only defaulting it when no status predicate contributes its own bound | Test-first development surfaced the bug before it shipped, exactly as `ai/gates/delivery.md`'s test-first rhythm intends | FR-07, FR-08 |
| 2026-09-19 | The human tested the built UI live and reported the Status/Desk dropdown popups rendering in a light theme against the app's dark theme, on both a desktop browser and Claude's own browser pane. Tried the standard CSS fix (`color-scheme: light dark` on the `<select>`) first — confirmed ineffective in this environment — then replaced the native `<select>` with a custom-rendered listbox (`role="listbox"`), which this stylesheet fully controls (`decisions.md` D-07) | A native `<select>`'s options popup is OS/browser chrome, not page content, and this platform does not honour `color-scheme` on it the way most Chromium versions do; the only reliable fix is not to depend on it | FR-16 |
| 2026-09-19 | The human asked for the date fields to use a rendered-HTML calendar matching the style already used for booking a desk, rather than the browser's native `<input type="date">` popup — superseding D-04. Built `Calendar.tsx` (screen-private), reusing `DatePicker`'s CSS chrome but not the component itself (still clamped to the booking window, which this filter must not be), and rewired `DateField` to open it as a popover with Clear/Today shortcuts | Direct human request after seeing the native popup live; D-04's native-input trade-off is no longer the shipped shape | D-08, FR-16 |
| 2026-09-19 | The human reported the calendar's last row looking misaligned after testing it live. The row's column alignment was in fact correct (28/29/30 under Mo/Tu/We); the blank trailing cells just read as visually cut short. Filled the first/last week's empty slots with dimmed, non-interactive adjacent-month day numbers instead (`decisions.md` D-09) | Direct human request after seeing the calendar live; `DatePicker` itself is unaffected — this is `Calendar`'s own rendering only | D-09, FR-16 |
| 2026-09-19 | With the adjacent-month days visible, the human then reported a REAL vertical alignment bug: date columns drifting from their Mo…Su header, worse toward the right. Root cause: the shared `date-picker.css`'s header row has no gap between its 7 labels while the grid rows do (`gap: var(--s-4)`) — two equal-width flex rows with different total gap width, drifting apart column by column. Fixed with a scoped override in this screen's own stylesheet rather than editing the shared, protected `date-picker.css` (`decisions.md` D-10) | Direct human report from live testing; the same latent drift likely also affects `DatePicker` on "Choose a booking date", flagged as a follow-up rather than fixed here since it's outside this story | D-10 |
